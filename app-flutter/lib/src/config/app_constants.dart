class AppConstants {
  static const String appName = "Distribuidora Clarita";

  // Tiempo para refrescar datos desde API
  static const Duration syncInterval = Duration(minutes: 5);

  // Nombre de la base SQLite local
  static const String dbName = "clarita_local.db";
}
